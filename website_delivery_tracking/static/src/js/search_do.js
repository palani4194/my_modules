
function updatetxt()
    {

    var input = document.getElementById("search_ip");
      var filter = input.value.toUpperCase();
      var tr_total_line = document.getElementsByTagName("tr");

      // 
      // for (i = 1; i <= tr_total_line.length-1; i++) {
      //   var td = tr_total_line[i].getElementsByTagName("td");
      //     for (i = 1; i <= 1; i++) {
      //           var a = td[i].getElementsByTagName("span")[0];
      //           if (a.innerHTML.toUpperCase().indexOf(filter) > -1)
      //             {
      //             alert("tr");
      //             debugger;
      //             tr_total_line[i].style.display = "block";
      //             }
      //             else
      //             {
      //             tr_total_line[i].style.display = "hidden";
      //
      //             }
      //           }
      //         }

    }
